.fb-btn
	= form_tag user_omniauth_authorize_path(:facebook), method: :get do
		= submit_tag "Sign in with Facebook", class: "btn btn-primary btn-large", data: { disable_with: "Connecting to facebook..." }
	=# link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook),class: "fb-btn"